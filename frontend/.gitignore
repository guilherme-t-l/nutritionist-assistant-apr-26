# Installed dependencies (recreated by `npm install` from package.json)
node_modules/

# Production build output (generated by `npm run build`)
dist/

# Vite's cache
.vite/

# Editor / OS noise
.DS_Store
*.log
